$kind: http-request
name: Retrieve Driver Safety Profile
url: "{{baseUrl}}/driver/:id"
method: GET
headers:
  Accept: application/json
pathVariables:
  id: "1001"
auth:
  type: bearer
  credentials:
    token: "{{vault:jwt_signed}}"
examples: ./.resources/Retrieve Driver Safety Profile.resources/examples
order: 2000

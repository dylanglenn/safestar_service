$kind: http-request
name: Display Custom Safety Message to Driver
url: "{{baseUrl}}/display"
method: GET
body:
  type: json
  content: |-
    {
      "driver": "{{driverID}}",
      "vehicle": "{{vehicleID}}"
    }
order: 4000
